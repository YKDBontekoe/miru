--- backend/app/domain/chat/background_service.py
+++ backend/app/domain/chat/background_service.py
@@ -15,7 +15,7 @@
 if TYPE_CHECKING:
     from app.infrastructure.database.models.agent_models import Agent
     from app.domain.chat.entities import ChatRoomEntity
-    from app.infrastructure.repositories.agent_repo import AgentRepository
+    from app.domain.agents.repository import AgentRepositoryInterface
     from app.infrastructure.repositories.chat_repo import ChatRepository
     from app.infrastructure.repositories.memory_repo import MemoryRepository

@@ -32,7 +32,7 @@

     def __init__(
         self,
-        agent_repo: AgentRepository,
+        agent_repo: AgentRepositoryInterface,
         memory_repo: MemoryRepository,
         agent_service: AgentService,
         chat_repo: ChatRepository,
--- backend/app/domain/chat/websocket_broadcaster.py
+++ backend/app/domain/chat/websocket_broadcaster.py
@@ -13,7 +13,7 @@
     from app.domain.agents.schemas import AgentResponse
     from app.domain.chat.entities import ChatMessageEntity
     from app.infrastructure.database.models.agent_models import Agent
-    from app.infrastructure.repositories.agent_repo import AgentRepository
+    from app.domain.agents.repository import AgentRepositoryInterface
     from app.infrastructure.repositories.chat_repo import ChatRepository

 logger = logging.getLogger(__name__)
@@ -22,7 +22,7 @@
 class ChatWebSocketBroadcaster:
     """Handles broadcasting events and messages over WebSocket to chat rooms."""

-    def __init__(self, chat_repo: ChatRepository, agent_repo: AgentRepository):
+    def __init__(self, chat_repo: ChatRepository, agent_repo: AgentRepositoryInterface):
         self.chat_repo = chat_repo
         self.agent_repo = agent_repo

--- backend/app/domain/chat/crew_orchestrator.py
+++ backend/app/domain/chat/crew_orchestrator.py
@@ -45,7 +45,7 @@
     from uuid import UUID

     from openai.types.chat import ChatCompletionMessageParam
-
-    from app.infrastructure.database.models.agent_models import Agent
+    from app.domain.agents.entities import AgentEntity

 logger = logging.getLogger(__name__)

@@ -188,7 +188,7 @@

     @staticmethod
     async def execute_crew_task(
-        room_agents: list[Agent],
+        room_agents: list[AgentEntity],
         user_message: str,
         user_id: UUID,
         room_id: UUID,
@@ -232,7 +232,7 @@

         for agent in room_agents:
             # Justification: capabilities list extraction assumes a tortoise m2m list or strings.
-            caps = agent.capabilities.related_objects if hasattr(agent.capabilities, "related_objects") else agent.capabilities
-            cap_ids = [c.id if hasattr(c, "id") else c for c in caps]
+            cap_ids = agent.capability_ids
             enabled_tools = [
                 tool for cap_id, tool in available_tools.items() if cap_id in cap_ids
             ]

--- backend/app/domain/chat/service.py
+++ backend/app/domain/chat/service.py
@@ -260,7 +260,7 @@
         # Pass to crew orchestrator if we have agents in the room
         result = await CrewOrchestrator.execute_crew_task(
             room_agents=db_agents,
             user_message=user_message,
             user_id=user_id,
             room_id=room_id,
